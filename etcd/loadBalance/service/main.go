package main

const (
	network = "tcp"
	addr = "localhost:8000"
)

func main()  {
	//listener, err := net.Listen(network, addr)
	//if err != nil {
	//	log.Fatal(err)
	//}
	//log.Println(addr + "net.Listening...")

}
